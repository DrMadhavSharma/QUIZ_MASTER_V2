if (name = subject.get("name")) in :
        #     app.security.datastore.create_user(name = subject["name"],
        #                                     id = subject["id"],
        #                                     )
        #     db.session.commit()
        #     return jsonify({
        #         "message": "Subject created successfully"
        #     }), 201
        
        # return jsonify({
        #     "message": "Subject already exists!"
        # }), 400